.git
autom4te.cache
config.log
config.status
configure~

# Generated by ./configure locally -- regenerated fresh inside the image by
# docker/*.Dockerfile's own ./autogen.sh && ./configure, so shipping the
# host's copies in the build context would just be dead weight (and, for
# the .la files, host-specific absolute paths).
Makefile
*/Makefile
*/*/Makefile
*.o
*.lo
*.la
.libs/
.deps/

.bench-install/
.bench-fio/
results/
